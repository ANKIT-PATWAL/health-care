- name : Configure Kubernetes on EC2 Instances
  hosts : 3.17.174.79
  become: true
  connection : ssh
  tasks : 
  - name: Create Deploy 
    command : sh kubectl create -f pod.yml
    become : yes
    become_user : root

  - name : Create Service
    command : sh kubectl create -f service.yml
    become : yes
    become_user : root
