- name : Configure Kubernetes on EC2 Instances
  hosts : 18.116.89.165
  become: true
  connection : ssh
  tasks :
   - name: Deploy in K8s
     k8s :
       state: present
       definition:
           apiVersion: v1
           kind: Pod
           metadata:
      name: nodeapp 
  labels:
    app: nodeapp
  spec:
      containers:
      - name: nodeapp
        image: ankit9090/medicure-image:latest
        ports:
        - containerPort: 8082
      -  name: Create Devlopment Namespace
            
-  name:  service expose  
      k8s:
      state: present
      difinition:
          apiVersion: v1
          kind: Service
          metadata:
   name: nodeapp
   spec:
  selector:
    app: nodeapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
  type: LoadBalancer
